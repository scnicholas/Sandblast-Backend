"use strict";

/** Cohesive coordinator for Layers 12–14. Metadata-only; no route authority. */
const outcome=require("./marionOutcomeAwareness.js");
const commitments=require("./marionCommitmentTracker.js");
const guidance=require("./marionAnticipatoryGuidance.js");
const VERSION="marion.outcomeFlowCoordinator/14.0-cohesive-12-13-14";
const CONTRACT="nyx.marion.outcomeFlow/1.0";
function isObj(v){return !!v&&typeof v==="object"&&!Array.isArray(v);}
function text(v,max=4000){try{return String(v==null?"":v).replace(/[\u0000-\u001f\u007f]/g," ").replace(/\s+/g," ").trim().slice(0,max);}catch(_){return"";}}
function first(){for(const v of arguments){const t=text(v);if(t)return t;}return"";}
function previousFrom(v={}){const x=isObj(v)?v:{};if(isObj(x.outcomeFlowState))return x.outcomeFlowState;if(isObj(x.outcomeFlow))return x.outcomeFlow;if(isObj(x.conversationFlowState)&&isObj(x.conversationFlowState.outcomeFlow))return x.conversationFlowState.outcomeFlow;if(isObj(x.privateRuntimeContinuity)&&isObj(x.privateRuntimeContinuity.outcomeFlowState))return x.privateRuntimeContinuity.outcomeFlowState;if(x.contract===CONTRACT||isObj(x.commitmentTracking)||isObj(x.lastOutcome))return x;return{};}
function threadId(flow={}){const f=isObj(flow)?flow:{},p=isObj(f.contextPivot),a=isObj(p.activeThread);return first(a.id,f.activeThreadId,f.threadId,`thread-${text(f.activeDomain||"general",60)}-${text(f.activeSubject||"general",100).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")||"general"}`);}
function analyzeTurn({prompt="",previous={},conversationFlow={},turnId=""}={}){
  const prev=previousFrom(previous),flow=isObj(conversationFlow)?conversationFlow:{},tid=threadId(flow),subject=first(flow.activeSubject,isObj(prev.lastOutcome)?prev.lastOutcome.relatedSubject:""),outcomeAwareness=outcome.classify(prompt,{turnId,threadId:tid,subject}),commitmentTracking=commitments.update({prompt,outcome:outcomeAwareness,previous:isObj(prev.commitmentTracking)?prev.commitmentTracking:prev,threadId:tid,subject,turnId}),guidanceOutcome=(outcomeAwareness.outcomeType==="none"&&guidance.isOutcomeQuery(prompt)&&isObj(prev.lastOutcome))?prev.lastOutcome:outcomeAwareness,anticipatoryGuidance=guidance.analyze({prompt,outcome:guidanceOutcome,commitments:commitmentTracking,conversationFlow:flow});
  return {version:VERSION,contract:CONTRACT,layers:Object.freeze({outcomeAwareness:12,commitmentTracking:13,anticipatoryGuidance:14}),turnId:text(turnId,120),threadId:text(tid,160),activeSubject:text(subject,320),outcomeAwareness,commitmentTracking,anticipatoryGuidance,lastOutcome:outcomeAwareness.outcomeType!=="none"?outcome.projectState(outcomeAwareness):isObj(prev.lastOutcome)?prev.lastOutcome:{},internalOnly:true};
}
function commitTurn(value={},reply="",result={}){const v=isObj(value)?value:{};return {...v,lastReply:text(reply,1200),resultStatus:text(isObj(result)?result.statusCode:"",40),committed:true,committedAt:Date.now()};}
function projectState(value={}){const v=isObj(value)?value:{};return {version:VERSION,contract:CONTRACT,threadId:text(v.threadId,160),activeSubject:text(v.activeSubject,320),outcomeAwareness:outcome.projectState(v.outcomeAwareness),commitmentTracking:commitments.projectState(v.commitmentTracking),anticipatoryGuidance:guidance.projectState(v.anticipatoryGuidance),lastOutcome:isObj(v.lastOutcome)?outcome.projectState(v.lastOutcome):{},lastReply:text(v.lastReply,1200),committed:v.committed===true,committedAt:Number(v.committedAt||0)};}
function genericReply(v=""){return /^(?:i['’]?m here|i am here|hello,? mac|tell me what you want|what would you like|ready to work through|start with the route-precedence chain)/i.test(text(v));}
function failureReply(v=""){return /\b(?:private runtime is unavailable|final envelope missing|diagnostic packet|non-final|bridge handoff|composer reply missing|turn did not complete cleanly)\b/i.test(text(v));}
function reconcileVisibleReply(reply="",value={}){const v=isObj(value)?value:{},g=isObj(v.anticipatoryGuidance)?v.anticipatoryGuidance:{},suggested=text(g.suggestedReply,1400),current=text(reply,12000);if(!suggested)return current;if(g.shouldAnswerDirectly===true||!current||genericReply(current)||failureReply(current))return suggested;return current;}
function reconcileResult(result={},value={}){if(!isObj(result))return result;const replyCandidates=[result.directReply,result.visibleReply,result.displayReply,result.finalReply,result.reply,result.text,result.message,isObj(result.finalEnvelope)?result.finalEnvelope.reply:"",isObj(result.payload)?result.payload.reply:""];let reply="";for(const x of replyCandidates){reply=text(x,12000);if(reply)break;}reply=reconcileVisibleReply(reply,value);const state=projectState(value),memory=isObj(result.memoryPatch)?result.memoryPatch:{},session=isObj(result.sessionPatch)?result.sessionPatch:{},meta=isObj(result.meta)?result.meta:{},finalEnvelope=isObj(result.finalEnvelope)?result.finalEnvelope:{},payload=isObj(result.payload)?result.payload:{};const base={...result,outcomeFlow:value,outcomeFlowState:state,outcomeAwareness:value.outcomeAwareness,commitmentTracking:value.commitmentTracking,anticipatoryGuidance:value.anticipatoryGuidance,memoryPatch:{...memory,outcomeFlowState:state},sessionPatch:{...session,outcomeFlowState:state},meta:{...meta,outcomeFlowVersion:VERSION,conversationLayers:[9,10,11,12,13,14]}};if(!reply)return base;return {...base,reply,text:reply,answer:reply,output:reply,response:reply,message:reply,directReply:reply,visibleReply:reply,displayReply:reply,finalReply:reply,payload:{...payload,reply,text:reply,message:reply},finalEnvelope:{...finalEnvelope,reply,text:reply,answer:reply,output:reply,response:reply,message:reply,directReply:reply,visibleReply:reply,displayReply:reply,finalReply:reply,outcomeFlowState:state}};}
function getStatus(){return {ok:true,version:VERSION,contract:CONTRACT,layers:{12:outcome.VERSION,13:commitments.VERSION,14:guidance.VERSION},routeAuthority:false,replyAuthority:false,metadataOnly:true,boundedCommitmentLedger:true,unauthorizedExecution:false};}
module.exports={VERSION,CONTRACT,previousFrom,threadId,analyzeTurn,commitTurn,projectState,reconcileVisibleReply,reconcileResult,getStatus,outcome,commitments,guidance};
