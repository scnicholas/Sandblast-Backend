'use strict';
const VERSION='nyx.lingosentinel.orderingBuffer/9.0-bounded';
class OrderingBuffer{constructor(o={}){this.max=Math.max(10,Math.min(500,Number(o.max)||50));this.expected=Math.max(1,Number(o.startSequence)||1);this.items=new Map();} push(item){const seq=Number(item&&item.sequence);if(!Number.isSafeInteger(seq)||seq<1)return{ok:false,error:'SEQUENCE_INVALID',released:[]};if(seq<this.expected)return{ok:true,duplicate:true,released:[]};if(this.items.size>=this.max&&!this.items.has(seq))return{ok:false,error:'ORDERING_BUFFER_FULL',released:[]};this.items.set(seq,item);const released=[];while(this.items.has(this.expected)){released.push(this.items.get(this.expected));this.items.delete(this.expected);this.expected+=1;}return{ok:true,released,gap:this.items.size>0?this.expected:null};} reset(start=1){this.items.clear();this.expected=Math.max(1,Number(start)||1);} getHealth(){return{ok:true,service:'LingoSentinelOrderingBuffer',version:VERSION,buffered:this.items.size,expected:this.expected,max:this.max};}}
module.exports={VERSION,OrderingBuffer};
