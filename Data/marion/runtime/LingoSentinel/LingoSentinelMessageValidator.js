'use strict';
const MessagePolicy=require('./LingoSentinelMessagePolicy');
const PublicProjection=require('./LingoSentinelPublicMessageProjection');
const RoomRegistry=require('./LingoSentinelRoomRegistry');
const ConnectionState=require('./LingoSentinelConnectionState');
const ReplayGuard=require('./LingoSentinelReplayGuard');
const VERSION='nyx.lingosentinel.messageValidator/10.0-integrity-recovery';
function validatePublishRequest(input={},context={},options={}){const policy=MessagePolicy.validateRawRequest(input);const errors=policy.errors.slice();const normalized=policy.normalized;const identity={clientId:String(context.clientId||'').trim(),sessionId:String(context.sessionId||'').trim(),membershipCredential:String(context.membershipCredential||'').trim()};if(!identity.clientId)errors.push({code:'CLIENT_ID_REQUIRED',field:'clientId'});if(!identity.sessionId)errors.push({code:'SESSION_ID_REQUIRED',field:'sessionId'});if(!identity.membershipCredential)errors.push({code:'MEMBERSHIP_CREDENTIAL_REQUIRED',field:'membershipCredential'});const replay=ReplayGuard.validatePublish(input,{roomId:normalized.roomId});errors.push(...replay.errors);let authorization={ok:false,error:'room_membership_required'};if(normalized.roomId&&identity.clientId&&identity.sessionId&&identity.membershipCredential){authorization=RoomRegistry.authorize(normalized.roomId,identity,'publish');if(!authorization.ok)errors.push({code:String(authorization.code||authorization.error||'ROOM_MEMBERSHIP_REQUIRED'),field:'roomId'});}let connection=null;if(options.requireConnected!==false&&identity.sessionId){connection=ConnectionState.get(identity.sessionId);if(!connection)errors.push({code:'CONNECTION_REGISTRATION_REQUIRED',field:'sessionId'});else{if(connection.clientId!==identity.clientId)errors.push({code:'CONNECTION_CLIENT_MISMATCH',field:'clientId'});if(connection.roomId!==normalized.roomId)errors.push({code:'CONNECTION_ROOM_MISMATCH',field:'roomId'});if(connection.state!=='connected')errors.push({code:'CONNECTION_NOT_READY',field:'sessionId'});}}return{ok:errors.length===0,errors,normalized,membership:authorization.membership||null,connection};}
function validateEnvelope(envelope={}){return PublicProjection.validateProjection(PublicProjection.project(envelope));}
function getHealth(){return{ok:true,service:'LingoSentinelMessageValidator',version:VERSION,membershipRequired:true,connectionRequired:true,replayGuard:ReplayGuard.getHealth(),recoveredEnvelopeValidation:true};}
module.exports=Object.freeze({VERSION,validatePublishRequest,validateEnvelope,getHealth});
