'use strict';
const BASE=(process.env.LS_BACKEND||'http://localhost:3000').replace(/\/$/,'');
const TOKEN=process.env.LS_WIDGET_TOKEN||'';
function headers(){return {'Content-Type':'application/json',...(TOKEN?{'x-sb-widget-token':TOKEN}:{})};}
async function request(sourceLanguage,targetLanguage,message){const r=await fetch(BASE+'/api/lingosentinel/marion/cognitive/request',{method:'POST',headers:headers(),body:JSON.stringify({sessionId:'phase3-live-'+sourceLanguage+targetLanguage,conversationId:'c1',sourceLanguage,targetLanguage,cultureContext:'general',layer:'language',mode:'one_to_one',speakerRole:'host',message,returnMode:'both'})});let j={};try{j=await r.json()}catch(_){}const x=j.response||{},ok=r.ok&&j.ok!==false&&!!x.canonicalResponse&&(targetLanguage==='en'?!!x.localizedResponse:!!x.localizedResponse);console.log(JSON.stringify({pair:sourceLanguage+'->'+targetLanguage,http:r.status,canonical:x.canonicalResponse||'',localized:x.localizedResponse||'',degraded:x.degraded===true,ok}));if(!ok)process.exitCode=1;}
(async()=>{const h=await fetch(BASE+'/api/lingosentinel/marion/cognitive/health');console.log('health',h.status);await request('en','fr','Good evening. How are you?');await request('fr','en','Bonsoir. Comment allez-vous ?');await request('en','en','This should remain English through the canonical path.');})().catch(e=>{console.error(e);process.exit(1)});
